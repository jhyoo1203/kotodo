spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kotodo
    username: jhyoo

  jpa:
    database: postgresql
    generate-ddl: true
    hibernate:
      ddl-auto: update